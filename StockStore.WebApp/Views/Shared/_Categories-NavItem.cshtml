@using StockStore.WebApp.Data.Repositories
@inject ICategoryRepository _categoryRepository

<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="dropdown09" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">گروه ها</a>
    <div class="dropdown-menu" aria-labelledby="dropdown09">
        @foreach (var item in _categoryRepository.GetAllCategories())
        {
            <a class="dropdown-item" href="/Category/@item.Id/@item.Name">@item.Name</a>
        }
    </div>
</li>